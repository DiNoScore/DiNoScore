using Gtk 4.0;
using Adw 1;

Adw.AlertDialog crash_dialog {
  heading: _("DiNoScore crashed :(");
  body-use-markup: true;
  default-response: "restart";

  responses [
    close: _("_Close"),
    restart: _("_Restart DiNoScore") suggested,
  ]
}
